# Cubecobra Printer

Create a pdf from a MTG cube from cubecobra.com

## Commands

Run app:

`go run .`

Run app without interaction (for testing only):

`go run . -dev`

Build binaries

`./build-all.sh`
